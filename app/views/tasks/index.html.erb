<h1>Task</h1>
<%= link_to 'Add Task', new_task_path %>
<hr />
<h2><%= @task.length <= 0 ? "No task" : "" %></h2> 
<ul>
  <% @task.each do |task| %>
    <li>
      <%= link_to task.name, task %>
      
    </li>
  <% end %>
</ul>